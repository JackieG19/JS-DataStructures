function spreadOut() {

  let fragment = ['to', 'code'];
  
  let sentence; // change this line
  
  return sentence = ['learning', ...fragment, 'is', 'fun'];
}

console.log(spreadOut());

/*
The spread operator hass the ability to combine arrays, or insert all the elements of one array into another at any index.
Can concatenate arrays, but this only allows to combine arrays at the end of one, and at the start of another*/
